import { FilterHeaderPipe } from './filter-header.pipe';

describe('FilterHeaderPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterHeaderPipe();
    expect(pipe).toBeTruthy();
  });
});
